# Which LLM provider backend.llm_client uses: "anthropic" or "gemini".
# Defaults to "gemini" when unset.
LLM_PROVIDER=gemini

# Required when LLM_PROVIDER=gemini.
GEMINI_API_KEY=

# Required when LLM_PROVIDER=anthropic.
ANTHROPIC_API_KEY=
